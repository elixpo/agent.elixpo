name: solve

# Maintainer-approved production targets flow through Solve and Submit in one
# isolated runner so the local fork workspace never becomes cross-workflow state.

on:
  repository_dispatch:
    types: [doctor_retry, oreoflow_approved]
  workflow_dispatch:
    inputs:
      issue_url:
        description: Writable allowlisted issue URL for owned test mode
        required: false
      owned_test:
        description: Use the explicit configured test repository
        type: boolean
        required: false
        default: false
      submit:
        description: Push the reviewed branch and open the PR
        type: boolean
        required: false
        default: true

permissions:
  contents: write

concurrency:
  group: state-write
  cancel-in-progress: false

jobs:
  solve:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event_name == 'repository_dispatch' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Decide whether a target is ready
        id: gate
        env:
          EVENT_NAME: ${{ github.event_name }}
          ISSUE_URL: ${{ inputs.issue_url }}
          OWNED_TEST: ${{ inputs.owned_test }}
          WANT_SUBMIT: ${{ inputs.submit }}
          DISPATCH_TYPE: ${{ github.event.action }}
          RETRY_FINGERPRINT: ${{ github.event.client_payload.fingerprint }}
        run: |
          run=false
          submit=true
          if [[ "$EVENT_NAME" == "workflow_dispatch" ]]; then
            if [[ "$OWNED_TEST" == "true" && -n "$ISSUE_URL" ]]; then
              run=true
            fi
            submit="${WANT_SUBMIT:-true}"
          elif [[ "$EVENT_NAME" == "repository_dispatch" ]]; then
            admission_ready=false
            if jq -e '.status == "approved"' state/admission.json >/dev/null \
              && jq -e '.status == "picked" and .picked == true' state/pick.json >/dev/null \
              && jq -e '.status == "approved" and .suitable == true and .test_mode != true' state/vet.json >/dev/null; then
              admission_ready=true
            fi
            if [[ "$DISPATCH_TYPE" == "oreoflow_approved" && "$admission_ready" == "true" ]] \
              && jq -e --arg fingerprint "$RETRY_FINGERPRINT" \
                '.fingerprint == $fingerprint' state/admission.json >/dev/null; then
              run=true
            elif [[ "$DISPATCH_TYPE" == "doctor_retry" && "$admission_ready" == "true" ]] \
              && jq -e --arg fingerprint "$RETRY_FINGERPRINT" \
                '.status == "decided" and .current.action == "retry" and .current.failure_fingerprint == $fingerprint and .current.retry_count == 1' \
                state/doctor.json >/dev/null; then
              run=true
            fi
          fi
          echo "run=$run" >> "$GITHUB_OUTPUT"
          echo "submit=$submit" >> "$GITHUB_OUTPUT"
      - uses: actions/setup-python@v5
        if: steps.gate.outputs.run == 'true'
        with:
          python-version: "3.11"
      - name: Setup Node verification runtime
        if: steps.gate.outputs.run == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Setup Node harness runtime
        if: steps.gate.outputs.run == 'true'
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Cache RTK
        if: steps.gate.outputs.run == 'true'
        id: rtk-cache
        uses: actions/cache@v4
        with:
          path: ~/.local/bin/rtk
          key: rtk-${{ runner.os }}-${{ runner.arch }}-v0.42.0
      - name: Install pinned RTK
        if: steps.gate.outputs.run == 'true'
        env:
          RTK_TELEMETRY_DISABLED: "1"
        run: |
          if [ "${{ steps.rtk-cache.outputs.cache-hit }}" != "true" ]; then
            curl -fsSL https://raw.githubusercontent.com/rtk-ai/rtk/v0.42.0/install.sh | sh
          fi
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          export PATH="$HOME/.local/bin:$PATH"
          rtk --version
      - name: Install
        if: steps.gate.outputs.run == 'true'
        run: |
          pip install -e . yamllint
          go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.7
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Solve
        id: solve
        if: steps.gate.outputs.run == 'true'
        env:
          AGENT_GITHUB_SOLVER_TOKEN: ${{ secrets.AGENT_GITHUB_SOLVER_TOKEN }}
          ELIXPO_POLLINATIONS_API_KEY: ${{ secrets.ELIXPO_POLLINATIONS_API_KEY }}
          ELIXPO_GITHUB_FORK_OWNER: ${{ vars.ELIXPO_GITHUB_FORK_OWNER }}
          ISSUE_URL: ${{ inputs.issue_url }}
          OWNED_TEST: ${{ inputs.owned_test }}
        run: |
          # Python starts CCR and invokes the same bounded Node harness used by
          # local `python -m agents.solve` runs inside the fork workspace.
          args=()
          if [[ "$OWNED_TEST" == "true" ]]; then
            args+=(--issue-url "$ISSUE_URL" --owned-test)
          fi
          python -m agents.solve "${args[@]}"
      - name: Submit
        id: submit
        if: steps.gate.outputs.run == 'true' && steps.gate.outputs.submit == 'true'
        env:
          AGENT_GITHUB_SOLVER_TOKEN: ${{ secrets.AGENT_GITHUB_SOLVER_TOKEN }}
          ELIXPO_POLLINATIONS_API_KEY: ${{ secrets.ELIXPO_POLLINATIONS_API_KEY }}
        run: python -m agents.submit
      - name: Register PR follow-up memory
        if: steps.gate.outputs.run == 'true' && steps.gate.outputs.submit == 'true'
        env:
          ELIXPOO_GIST_AGENTIC_TOKEN: ${{ secrets.ELIXPOO_GIST_AGENTIC_TOKEN }}
          ELIXPOO_FOLLOWUP_GIST_ID: ${{ vars.ELIXPOO_FOLLOWUP_GIST_ID }}
          ELIXPO_FOLLOWUP_TTL_DAYS: ${{ vars.ELIXPO_FOLLOWUP_TTL_DAYS || 360 }}
        run: python -m agents.steward.remember
      - name: Clean submitted workspace
        id: janitor_success
        if: always() && steps.submit.outcome == 'success'
        env:
          ELIXPO_WORKSPACE_DIR: /tmp/elixpoo-workspaces
        run: python -m agents.janitor
      - name: Diagnose failed Solve
        id: doctor
        if: always() && steps.gate.outputs.run == 'true' && steps.solve.outcome == 'failure'
        run: python -m agents.doctor
      - name: Clean Doctor-authorized resources
        id: janitor_failure
        if: always() && steps.doctor.outcome == 'success'
        env:
          ELIXPO_WORKSPACE_DIR: /tmp/elixpoo-workspaces
        run: python -m agents.janitor
      - name: Commit state
        if: always() && steps.gate.outputs.run == 'true'
        env:
          CONTROL_TOKEN: ${{ secrets.ELIXPOO_GITHUB_AGENTIC_TOKEN }}
        run: |
          git config user.name "elixpoo"
          git config user.email "elixpoo@gmail.com"
          git add state/solve.json state/ledger.json state/contracts.json
          if [[ -f state/submit.json ]]; then git add state/submit.json; fi
          if [[ -f state/doctor.json ]]; then git add state/doctor.json; fi
          if [[ -f state/janitor.json ]]; then git add state/janitor.json; fi
          git diff --cached --quiet || git commit -m "chore(solve): record implementation run"
          auth=$(printf 'x-access-token:%s' "$CONTROL_TOKEN" | base64 -w 0)
          git -c http.extraheader="AUTHORIZATION: basic $auth" push
      - name: Dispatch one Doctor-authorized retry
        if: always() && steps.doctor.outputs.action == 'retry' && steps.janitor_failure.outcome == 'success'
        env:
          GH_TOKEN: ${{ secrets.ELIXPOO_GITHUB_AGENTIC_TOKEN }}
          FINGERPRINT: ${{ steps.doctor.outputs.fingerprint }}
          RETRY_AFTER_SECONDS: ${{ steps.doctor.outputs.retry_after_seconds }}
        run: |
          delay=$(( RETRY_AFTER_SECONDS > 60 ? 60 : RETRY_AFTER_SECONDS ))
          if (( delay > 0 )); then sleep "$delay"; fi
          gh api --method POST "repos/${{ github.repository }}/dispatches" \
            -f event_type=doctor_retry \
            -F "client_payload[fingerprint]=$FINGERPRINT"
